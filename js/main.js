
// var x = ['a','b','c']

// function y(v) {
//      if(x.includes(v)){
//          return (v + " is available")
//      } else {
//          return (v + " is not availbale")
//      }
//   }

var cars = ["Volvo", "Benz", "Porsche", "Mazda"]

 function carType(car) {
     if (cars.includes(car)){
         return (car + " is available");
     } else {
         return (car + "  is not available");
     }
 }










